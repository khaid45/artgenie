services:
  - type: web
    name: artgenie
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py  # ⬅️ Replace this if your entry point is different
    pythonVersion: 3.10.12
